#ifndef GENERAL_INC_HPP
#define GENERAL_INC_HPP

#include <FDPS_Header/particle_simulator.hpp>
#include <iostream>
#include "TRngPool.hpp"
#include "catch.hpp"

#define ABS(x)             ((x) < 0 ? -(x) : (x))
#define SQR(x)             ((x) * (x))
#define CUBE(x)            ((x) * (x) * (x))
#define SMALL 1e-8

#endif /* end of include guard: GENERAL_INC_HPP */
